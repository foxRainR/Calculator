
public class main {

	public static void main(String[] args) {
		// Accept user input of (number) (operator) (number)
		// Store numbers into variables
		// Read which operator was used and jump to the corresponding method
		
		int x = //input one
		int y = //input two
		

	}
	// Add
	static int add(int x, int y) {
		int sum = x + y;
		return sum;
	}
	// Subtract
	static int sub(int x, int y) {
		int difference = x - y;
		return difference;
	}
	// Multiply
	static int mul(int x, int y) {
		int product = x * y;
		return product;
	}
	// Divide
	static int div(int x, int y) {
		int quotient = x / y;
		return quotient;
	}
}
